# 影视评论与电影数据分析系统技术文档

## 1. 项目概述

本项目是一个基于 Flask 的数据分析 Web 应用，围绕影视评论和电影基础数据提供以下能力：

- 用户登录与注册
- 评论情感分析（模型推理 + 规则兜底）
- 评论主题分析（LDA）
- 评论时间段可视化（按年、月、小时）
- 演员/导演类型画像分析
- 电影语言、地区、年份、评分-时长、评分-类型分析

系统采用后端渲染模板（Jinja2）+ Chart.js 图表展示，数据存储在 SQLite。

---

## 2. 技术栈

### 2.1 后端与算法

- Python 3.13（当前运行环境）
- Flask 3.1.3
- sqlite3（标准库）
- jieba 0.42.1（中文分词）
- scikit-learn 1.8.0（LDA）
- numpy 2.4.2
- transformers 5.3.0
- torch 2.11.0

### 2.2 前端

- Jinja2 模板引擎
- 原生 JavaScript
- Chart.js（CDN）
- CSS（按页面拆分）

---

## 3. 目录结构

```text
e:\movie_analyze
├─ backend
│  ├─ analysis            # NLP 主题分析
│  ├─ db                  # 数据库访问层
│  ├─ routes              # 业务路由
│  └─ utils               # 工具函数
├─ static
│  ├─ css                 # 页面样式
│  └─ js                  # 页面脚本
├─ templates              # Jinja2 模板
├─ main.py                # 应用入口
├─ requirements.txt       # 依赖清单
└─ database.db            # SQLite 数据库
```

---

## 4. 系统架构说明

### 4.1 分层结构

- 路由层（`backend/routes`）：接收请求、参数处理、调用数据和分析模块、渲染模板
- 数据层（`backend/db/database.py`）：统一 SQL 查询、分页、统计、聚合
- 分析层（`backend/analysis`）：LDA 主题分析、文本预处理
- 展示层（`templates` + `static`）：页面渲染和图表绘制

### 4.2 请求流程（典型）

1. 浏览器访问路由（如 `/dashboard`）
2. 路由读取筛选参数并调用 `database.py` 获取数据
3. 如需分析，调用分析函数（例如情感评分、LDA）
4. 将结构化结果注入模板，前端用 Chart.js 可视化

---

## 5. 配置与运行

## 5.1 安装依赖

```bash
pip install -r requirements.txt
```

## 5.2 启动项目

```bash
python main.py
```

默认访问地址：

- `http://127.0.0.1:5000`

## 5.3 关键环境变量

- `SENTIMENT_MODEL_NAME`：情感模型名称或本地目录（默认 `lxyuan/distilbert-base-multilingual-cased-sentiments-student`）
- `SENTIMENT_PROXY`：模型下载代理地址（可选）

---

## 6. 路由与功能清单

### 6.1 认证与会话

- `GET/POST /auth`：登录/注册页
- `POST /login`：登录
- `POST /register`：注册
- `GET /logout`：退出登录
- `POST /generate_captcha`：生成验证码

### 6.2 业务页面

- `GET/POST /dashboard`：评论情感分析 + 个人设置
- `GET/POST /topic_analysis`：LDA 主题分析
- `GET/POST /chart_analysis`：评论时间段可视化
- `GET/POST /character_analysis`：演员/导演类型分析（分页、检索）
- `GET /movie_analysis`：电影综合分析
- `GET /api/movie_analysis/genre_ratings`：按类型返回电影评分散点数据

### 6.3 设置页兼容路由

- `GET/POST /settings`
- `POST /change_password`
- `POST /change_email`

---

## 7. 核心模块说明

## 7.1 评论情感分析（`dashboard.py`）

- 优先使用 HuggingFace 模型进行情感打分
- 当模型不可用时，自动切换规则兜底打分（关键词 + 否定词）
- 计算精确率、召回率、F1：
  - 真实正向阈值：评分 `>= 3.5`
  - 预测正向阈值：模型模式 `>= 3.5`，兜底模式 `>= 3.0`

## 7.2 评论主题分析（`topic_analysis.py` + `lda_analysis.py`）

- 对评论文本分词、停用词过滤、词性过滤
- 使用 LDA 自动选择主题数（默认 3~8）
- 输出主题词、主题分布、困惑度、连贯性、词云数据

## 7.3 评论时间段可视化（`chart_analysis.py`）

- 统计维度：
  - 按年评论量
  - 按月评论量（支持年份筛选）
  - 按月小时评论量（支持年份+月份）
  - 按年小时评论量（支持年份）
- 页面支持筛选后自动滚动回触发图表（`scroll_target`）

## 7.4 演员导演分析（`character_analysis.py`）

- 支持按姓名关键词检索与分页
- 详情接口：`/character_details/<character_id>`

## 7.5 电影数据分析（`movie_analysis.py`）

- 语言分布、地区分布、年份分布
- 评分-时长关系（相关系数、趋势线、异常值）
- 评分-类型关系（类型均分、类型样本量、指定类型散点）

---

## 8. 数据库设计（当前代码使用）

## 8.1 创建表（应用初始化）

- `users`
  - `id`, `username`, `password`, `email`
- `comment_movie`
  - `id`, `name`, `movie_id`, `comment_id`, `content`, `rating`

## 8.2 查询依赖的业务表

以下表由历史数据导入或外部脚本准备，代码直接查询：

- `comments_by_year`
- `comments_by_month`
- `month_hour_comments`
- `year_hour_comments`
- `character_simple_analysis`
- `character_analysis`
- `movie`
- `movies_score_mins`
- `movies_douban_genres`

---

## 9. 安全与稳定性说明

## 9.1 当前实现现状

- 密码哈希使用 MD5（`utils.hash_password`）
- `main.py` 默认 `debug=True`
- `secret_key` 写在代码中

## 9.2 建议优先改造

1. 使用 `bcrypt/argon2` 替换 MD5
2. 将 `secret_key` 和运行模式改为环境变量配置
3. 生产环境关闭 debug，使用 Gunicorn/Waitress 等 WSGI 服务
4. 增加统一日志与异常告警

---

## 10. 已知问题与注意事项

1. 部分历史页面/注释仍存在中文编码混乱（不影响核心功能但影响可维护性）
2. 若网络受限，HuggingFace 模型可能加载失败，系统会自动走规则兜底
3. 5000 端口被旧进程占用时，需先停止旧进程再重启

---

## 11. 开发与维护建议

1. 增加统一 DTO/Schema，减少路由中临时字段拼装
2. 为关键路由增加测试（情感指标计算、分页、图表筛选参数）
3. 将 SQL 按业务拆分，避免 `database.py` 单文件过大
4. 增加数据字典文档（字段含义、来源、刷新周期）

---

## 12. 文档信息

- 文档名称：项目技术文档
- 更新日期：2026-03-27
- 对应版本：当前仓库代码（`e:\movie_analyze`）
